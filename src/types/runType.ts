export interface RunType {
  runID: string;
  start: string;
  end: string;
  duration: string;
  triggerEvent: string;
  commitID: string;
  commitHash: string;
  commitMessage: string;
  status: string;
}